<%= form_with(model: library_zone, local: true) do |form| %>
  <% if library_zone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(library_zone.errors.count, "error") %> prohibited this library_zone from being saved:</h2>

      <ul>
      <% library_zone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :library_zone_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
